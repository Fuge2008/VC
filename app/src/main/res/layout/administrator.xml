<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

    <EditText
        android:id="@+id/edit_admin_name"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@null"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:hint="请输入管理员名称"
        android:textColorHint="#CCCCCC"
        android:maxLength="11"
        android:paddingLeft="90dp"/>

    <TextView
        android:id="@+id/tv_admin_name"
        android:textColor="#666666"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/edit_admin_name"
        android:layout_alignLeft="@+id/edit_admin_name"
        android:layout_alignTop="@+id/edit_admin_name"
        android:gravity="center_vertical"
        android:paddingLeft="13dp"
        android:text="管理员"
        android:drawablePadding="10dp"
        android:textSize="18sp"/>


    <View
        android:layout_below="@id/tv_admin_name"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginLeft="14dp"
        android:layout_marginRight="13dp"
        android:background="#D8D8D8"/>

</RelativeLayout>